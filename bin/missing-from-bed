#!/usr/bin/env

set -euo pipefail
IFS=$'\n\t'
BIN=$(dirname $0)

bed=$1
gff=$2

bed_upi=$(mktemp)
gff_upi=$(mktemp)

cut -f4 "$bed" | sort -u > $bed_upi
$BIN/upi-from-gff $gff > $gff_upi
comm -1 -3 $bed_upi $gff_upi | grep -vf $bad
